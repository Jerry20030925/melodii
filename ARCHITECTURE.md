# Melodii - 项目架构说明

## 项目概述

Melodii是一款基于iOS的社交媒体应用，使用SwiftUI构建前端界面，Supabase作为后端服务（PostgreSQL数据库 + RESTful API + 认证 + 文件存储）。

## 技术栈

- **前端**: SwiftUI + SwiftData
- **后端**: Supabase (PostgreSQL + PostgREST + GoTrue Auth + Storage)
- **认证**: Apple Sign In + Supabase Auth
- **语言**: Swift 5.9+
- **最低系统**: iOS 17.0+

## 项目结构

```
Melodii/
├── Melodii/
│   ├── MelodiiApp.swift              # 应用入口
│   ├── ContentView.swift             # 主TabView容器
│   │
│   ├── Models.swift                  # 数据模型
│   │   ├── User                      # 用户模型
│   │   ├── Post                      # 帖子模型
│   │   ├── Comment                   # 评论模型
│   │   ├── Like                      # 点赞模型
│   │   ├── Collection                # 收藏模型
│   │   ├── Notification              # 通知模型
│   │   └── Topic                     # 话题模型
│   │
│   ├── Views/                        # 视图层
│   │   ├── DiscoverView.swift        # 发现页（推荐流）
│   │   ├── CreateView.swift          # 发帖页
│   │   ├── MessagesView.swift        # 消息中心
│   │   └── ProfileView.swift         # 个人页
│   │
│   ├── Services/                     # 服务层
│   │   ├── SupabaseService.swift    # 数据库操作服务
│   │   ├── AuthService.swift         # 认证服务
│   │   └── StorageService.swift      # 文件存储服务
│   │
│   └── SupabaseConfig.swift          # Supabase配置
│
├── supabase_schema.sql               # 数据库架构SQL脚本
├── SETUP.md                          # 项目配置指南
└── ARCHITECTURE.md                   # 本文件
```

## 架构设计

### 1. 数据流架构

```
┌─────────────────┐
│   SwiftUI View │  (用户界面)
└────────┬────────┘
         │ @StateObject / @ObservedObject
         ↓
┌────────────────┐
│  Service Layer │  (业务逻辑)
│  @MainActor    │
└────────┬───────┘
         │ async/await
         ↓
┌────────────────┐
│ Supabase Swift │  (SDK)
│     Client     │
└────────┬───────┘
         │ HTTP/WebSocket
         ↓
┌────────────────┐
│   Supabase     │  (后端服务)
│   Backend      │
└────────────────┘
```

### 2. 服务层设计

#### **SupabaseService**
负责所有数据库CRUD操作：
- 帖子管理（创建、查询、更新、删除）
- 评论管理
- 点赞/收藏操作
- 通知管理
- 话题查询
- 用户信息管理

**特点**：
- 使用`@MainActor`确保UI更新在主线程
- 所有方法使用`async/await`异步编程
- 统一错误处理机制

#### **AuthService**
负责用户认证和会话管理：
- Apple Sign In集成
- 会话状态管理
- 用户登录/登出
- 自动会话恢复

**状态管理**：
```swift
@Published var currentUser: User?
@Published var isAuthenticated: Bool
```

#### **StorageService**
负责文件上传和管理：
- 图片上传（压缩、批量上传）
- 头像上传
- 文件删除
- URL管理

### 3. 数据模型设计

#### **核心实体关系**

```
User (用户)
  ├─ posts (1:N) → Post
  └─ comments (1:N) → Comment

Post (帖子)
  ├─ author (N:1) → User
  ├─ comments (1:N) → Comment
  ├─ likes (1:N) → Like
  └─ collections (1:N) → Collection

Comment (评论)
  ├─ author (N:1) → User
  ├─ post (N:1) → Post
  ├─ replyTo (N:1) → Comment (自关联)
  └─ likes (1:N) → Like

Notification (通知)
  ├─ user (N:1) → User (接收者)
  └─ actor (N:1) → User (触发者)
```

#### **模型特点**

1. **混合存储策略**
   - SwiftData：本地缓存（User, Post, Comment）
   - Supabase：云端持久化
   - 支持Codable协议实现序列化

2. **字段命名映射**
   - Swift使用camelCase：`createdAt`
   - PostgreSQL使用snake_case：`created_at`
   - 通过`CodingKeys`自动映射

### 4. 数据库架构

#### **表设计**

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| users | 用户信息 | id, apple_user_id, nickname |
| posts | 帖子内容 | id, author_id, text, media_urls, topics |
| comments | 评论 | id, post_id, author_id, text |
| likes | 点赞记录 | user_id, post_id, comment_id |
| collections | 收藏记录 | user_id, post_id |
| notifications | 通知 | user_id, actor_id, type |
| topics | 话题标签 | name, post_count |
| reports | 举报记录 | reporter_id, reason, status |
| messages | 私信 | sender_id, receiver_id, text |

#### **安全策略（RLS）**

Supabase的Row Level Security确保数据安全：

- **用户表**: 所有人可读，用户只能修改自己的信息
- **帖子表**: 已发布内容公开，用户管理自己的帖子
- **评论表**: 公开可读，用户管理自己的评论
- **点赞/收藏**: 用户只能操作自己的记录
- **通知**: 用户只能看到自己的通知
- **私信**: 用户只能看到自己相关的消息

#### **数据库函数**

自动计数更新函数：
```sql
- increment_post_like_count()      # 增加帖子点赞数
- decrement_post_like_count()      # 减少帖子点赞数
- increment_post_comment_count()   # 增加帖子评论数
- increment_comment_like_count()   # 增加评论点赞数
...
```

### 5. 认证流程

```
用户点击"使用Apple登录"
         ↓
显示Apple登录界面
         ↓
用户完成Face ID/Touch ID验证
         ↓
获取Apple ID Token + Nonce
         ↓
发送到Supabase Auth验证
         ↓
Supabase返回Session
         ↓
检查用户是否存在数据库
         ├─ 存在: 加载用户信息
         └─ 不存在: 创建新用户
         ↓
更新本地状态（isAuthenticated = true）
         ↓
跳转到主界面
```

### 6. 文件存储架构

**Storage Bucket结构**：
```
media/
├── {user_id}/
│   ├── avatar.jpg              # 用户头像
│   ├── {uuid}-1.jpg            # 帖子图片
│   ├── {uuid}-2.jpg
│   └── ...
└── ...
```

**上传流程**：
1. 选择图片 → PhotosPicker
2. 压缩图片 → JPG 80%质量
3. 上传到Supabase Storage → `media/{userId}/{uuid}.jpg`
4. 获取公开URL
5. 保存URL到数据库

### 7. UI层架构

#### **主要视图组件**

**DiscoverView（发现页）**
- 职责：展示推荐帖子流
- 数据源：`SupabaseService.fetchPosts()`
- 子组件：
  - `PostRowView`：帖子列表项
  - `PostDetailView`：帖子详情

**CreateView（发帖页）**
- 职责：创建新帖子
- 功能：
  - 文本输入
  - 图片选择（最多9张）
  - 话题标签添加
- 数据流：
  1. 上传图片 → StorageService
  2. 创建帖子 → SupabaseService

**ProfileView（个人页）**
- 职责：用户信息和个人内容管理
- 功能：
  - 显示用户信息
  - 我的帖子列表
  - 收藏、草稿入口
  - 登录/登出

**MessagesView（消息中心）**
- 职责：通知和私信（待实现）

### 8. 状态管理

#### **全局状态**

使用`@MainActor` + `ObservableObject`管理全局状态：

```swift
@StateObject private var authService = AuthService.shared
@StateObject private var supabaseService = SupabaseService.shared
```

#### **本地状态**

使用`@State`管理视图级别状态：

```swift
@State private var posts: [Post] = []
@State private var isLoading = false
@State private var showAlert = false
```

### 9. 错误处理

统一的错误处理策略：

```swift
do {
    let result = try await service.someOperation()
    // 处理成功结果
} catch {
    // 显示友好的错误提示
    alertMessage = error.localizedDescription
    showAlert = true
}
```

### 10. 性能优化考虑

**已实现**：
- 图片压缩（JPEG 80%）
- 异步加载（async/await）
- 懒加载视图（LazyVStack将来可用）

**待实现**：
- 分页加载（limit + offset）
- 图片缓存
- 列表虚拟化
- 数据预取

## MVP功能映射

### P0核心功能（两周）

| 功能 | 状态 | 涉及组件 |
|------|------|----------|
| Apple登录 | ✅ 已实现 | AuthService, ProfileView |
| 发现页推荐流 | ✅ 已实现 | DiscoverView, SupabaseService |
| 发帖（图文） | ✅ 已实现 | CreateView, StorageService |
| 评论 | 🚧 部分实现 | SupabaseService（API已有）|
| 点赞 | 🚧 部分实现 | SupabaseService（API已有）|
| 个人页 | ✅ 已实现 | ProfileView |
| 通知（点赞/评论）| 🚧 部分实现 | SupabaseService（API已有）|
| 基础审核/举报 | ⏳ 待实现 | 数据表已创建 |

### P1扩展功能

| 功能 | 状态 | 优先级 |
|------|------|--------|
| 话题系统 | 🚧 数据层完成 | 高 |
| 搜索 | ⏳ 待实现 | 高 |
| 收藏 | 🚧 API完成 | 中 |
| 草稿箱 | ⏳ 待实现 | 中 |
| 私信 | 🚧 数据层完成 | 低 |

## 扩展方向

### 短期（1-2周）

1. **完善交互功能**
   - 点赞按钮UI和状态管理
   - 评论输入和展示
   - 通知中心页面

2. **内容审核**
   - 举报按钮和表单
   - 敏感词过滤
   - 内容状态管理

### 中期（1个月）

1. **社交增强**
   - 用户关注系统
   - 个人主页优化
   - 用户搜索

2. **内容发现**
   - 话题页面
   - 搜索功能
   - 热门推荐算法

### 长期（3个月+）

1. **高级功能**
   - 视频支持
   - 直播功能
   - 地理位置

2. **商业化**
   - 广告系统
   - 付费会员
   - 创作者激励

## 技术债务和优化

### 当前已知问题

1. **数据同步**
   - SwiftData和Supabase双重存储需要同步机制
   - 考虑使用Supabase Realtime实现实时更新

2. **缓存策略**
   - 缺少图片缓存
   - 缺少数据缓存失效机制

3. **错误处理**
   - 需要统一的错误类型定义
   - 需要更友好的错误提示

### 优化建议

1. **性能**
   - 实现列表分页
   - 添加图片懒加载
   - 优化数据库查询（索引、联表）

2. **用户体验**
   - 添加骨架屏
   - 优化加载状态
   - 添加下拉刷新

3. **代码质量**
   - 添加单元测试
   - 使用依赖注入
   - 模块化拆分

## 开发建议

### 最佳实践

1. **遵循SwiftUI最佳实践**
   - 保持View轻量级
   - 使用ViewModels处理业务逻辑
   - 适当使用@State/@StateObject

2. **异步编程**
   - 统一使用async/await
   - 避免回调地狱
   - 正确处理Task生命周期

3. **安全性**
   - 不在客户端存储敏感信息
   - 使用Supabase RLS保护数据
   - 验证所有用户输入

### 调试技巧

1. **使用Xcode断点**
2. **查看Supabase日志**
3. **使用Network Link Conditioner测试弱网**
4. **真机测试认证流程**

---

## 相关文档

- [SETUP.md](./SETUP.md) - 项目配置指南
- [supabase_schema.sql](./supabase_schema.sql) - 数据库架构

---

**维护者**: Claude Code
**最后更新**: 2025-10-30
